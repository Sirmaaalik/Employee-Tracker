//  Here is where all the functions will be executed
const mysql = require('mysql2');

switch (functions) {
    case 'View all Departments':
        return (
            SELECT
        )
        break;
}